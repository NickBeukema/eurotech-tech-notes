<%= form_for(@repair_order_detail) do |f| %>
  <% if @repair_order_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repair_order_detail.errors.count, "error") %> prohibited this repair_order_detail from being saved:</h2>

      <ul>
      <% @repair_order_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cause %><br>
    <%= f.text_area :cause %>
  </div>
  <div class="field">
    <%= f.label :correction %><br>
    <%= f.text_area :correction %>
  </div>
  <div class="field">
    <%= f.label :parts %><br>
    <%= f.text_area :parts %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
